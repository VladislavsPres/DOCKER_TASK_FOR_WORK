# DOCKER_TASK_FOR_WORK
Test task with docker
## Project structure:
      app:
        dockerfile
        main.py
      db:
        init.sql
      nginx:
        ssl:
          selfsigned.crt
          selfsigned.key
        default.conf
      docker-compose.yml
